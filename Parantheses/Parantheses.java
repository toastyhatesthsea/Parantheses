package Parantheses;

public class Parantheses
{


    public boolean isValid(String s)
    {
        for (int i = 0; i < s.length(); i++)
        {
            String aParan = s.substring(i, i + 1);
        }
    }

    public boolean validParanHelper(String aString)
    {

    }

}
